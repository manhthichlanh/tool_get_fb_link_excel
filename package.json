{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node exportCSV && node index.js"
  },
  "dependencies": {
    "csv-parser": "^3.0.0",
    "fs": "^0.0.1-security",
    "nodemon": "^3.1.4",
    "xlsx": "^0.18.5"
  }
}
